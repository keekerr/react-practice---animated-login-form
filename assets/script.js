// needs for the app
// changing message depedning on if you have finished the game or not
// Alert Windows rps choicce for user, computer choice, win or loss, total count of wins losses or ties (must store historical info)
// opportunity for you play again

// Prompt Boxes (user)
var options = ["Rock, Paper, Scisors"];

function game() {
    var userChoice = window.prompt("Enter Rock, Paper, or Scisors")
    if (!userChoice) {
        return;
    }
}
// Pop up Boxes (Computer Choice, stored results)
// Message on the Page Change